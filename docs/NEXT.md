# NEXT

## 当前保留议题

- `/admin/models` 之外的后台运行状况、错误日志和更完整的运维面板暂缓，不和当前多模型能力一起推进。
- 快速会话模式、深度研究模式选择仍未立项，等待主会话链路与失败恢复体验进一步稳定后再评估。
- raw thinking 现已并入统一 process timeline；后续是否收敛成更可控的摘要/标签展示，而不是直接暴露片段，等待主会话链路进一步稳定后再评估。
- 当 workspace 已有可检索本地资料时，是否再增加一次显式 retrieval preflight 或更强的硬 gating；当前已经有 searchableKnowledge summary + agent prompt 强化“附件/本地知识优先”，但还没有独立 preflight。
