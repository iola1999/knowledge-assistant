{
  "name": "anchordesk",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "node scripts/dev-up.mjs",
    "dev:status": "node scripts/dev-status.mjs",
    "dev:down": "node scripts/dev-down.mjs",
    "dev:web": "node scripts/run-with-system-settings.mjs --require-env DATABASE_URL,AUTH_SECRET -- pnpm --filter @anchordesk/web dev",
    "dev:worker": "node scripts/run-with-system-settings.mjs --require-env DATABASE_URL -- pnpm --filter @anchordesk/worker dev",
    "dev:agent": "node scripts/run-with-system-settings.mjs --require-env DATABASE_URL -- pnpm --filter @anchordesk/agent-runtime dev",
    "infra:up": "docker compose up -d postgres redis qdrant minio",
    "infra:down": "docker compose stop postgres redis qdrant minio",
    "infra:logs": "docker compose logs -f postgres redis qdrant minio",
    "build:web": "pnpm --filter @anchordesk/web build",
    "setup:python": "sh -c 'PYTHON_BIN=$(command -v python3.12 || command -v python3.13 || command -v python3); \"$PYTHON_BIN\" -m venv .venv && ./.venv/bin/python -m pip install --upgrade pip && ./.venv/bin/python -m pip install -r services/parser/requirements.txt && node scripts/write-parser-requirements-stamp.mjs'",
    "test": "pnpm test:ts && pnpm test:python",
    "test:ts": "vitest run",
    "test:python": "sh -c 'PYTHON=.venv/bin/python; if [ ! -x \"$PYTHON\" ]; then PYTHON=python3; fi; \"$PYTHON\" -m unittest discover -s services/parser -p \"test_*.py\"'",
    "test:watch": "vitest",
    "coverage:ts": "vitest run --coverage",
    "coverage:python": "sh -c 'PYTHON=.venv/bin/python; if [ ! -x \"$PYTHON\" ]; then PYTHON=python3; fi; \"$PYTHON\" -m coverage run -m unittest discover -s services/parser -p \"test_*.py\" && \"$PYTHON\" -m coverage report'",
    "coverage": "pnpm coverage:ts && pnpm coverage:python",
    "check:python": "sh -c 'PYTHON=.venv/bin/python; if [ ! -x \"$PYTHON\" ]; then PYTHON=python3; fi; \"$PYTHON\" -m py_compile services/parser/main.py services/parser/ocr.py services/parser/parser_utils.py services/parser/test_parser_utils.py services/parser/test_main.py'",
    "typecheck": "pnpm -r --if-present typecheck",
    "verify": "pnpm test && pnpm typecheck && pnpm check:python && pnpm build:web",
    "db:generate": "pnpm --filter @anchordesk/db generate",
    "db:migrate": "pnpm --filter @anchordesk/db migrate",
    "app:upgrade:dev": "node scripts/upgrade-run.mjs --mode=apply-safe-blocking",
    "app:upgrade": "node scripts/upgrade-run.mjs --mode=apply-blocking",
    "app:upgrade:all": "node scripts/upgrade-run.mjs --mode=apply-all",
    "app:upgrade:check": "node scripts/upgrade-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@types/pg": "^8.20.0",
    "@vitest/coverage-v8": "^4.1.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
